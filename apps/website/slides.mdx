---
layout: center
---

# reslide

React + MDX ベースのプレゼンテーションフレームワーク

← → キーまたはスワイプでスライド移動 / Space で次へ

---

## 特徴

::click

- **MDX** でスライドを記述 — Markdown の手軽さ + React コンポーネントの表現力

::click

- **フレームワーク非依存** — Next.js / Vite / Remix 等あらゆる React プロジェクトで動作

::click

- **16:9 レスポンシブ** — PC でもスマホでも同じレイアウトで表示

---

---

## layout: two-cols

## レイアウトシステム

`layout` プロパティで多彩なレイアウトを切り替え

```tsx
---
layout: two-cols
---

左カラムの内容

<SlotRight>
右カラムの内容
</SlotRight>
```

<SlotRight>

### 組み込みレイアウト

- `default` — 標準
- `center` — 中央寄せ
- `two-cols` — 2カラム
- `image-right` / `image-left`
- `section` — セクション区切り
- `quote` — 引用

</SlotRight>

---

---

## layout: section

# リッチコンテンツ

数式・ダイアグラム・ハイライトをネイティブサポート

---

## テキストハイライト — Mark

::click

重要な箇所を :highlight-yellow[ハイライト] で強調

::click

キーワードを :underline-blue[アンダーライン] で示す

::click

注目ポイントを :circle-red[サークル] で囲む

```
:highlight-yellow[ハイライト]
:underline-blue[アンダーライン]
:circle-red[サークル]
```

---

## 数式サポート — KaTeX

インライン数式: $E = mc^2$ や $a^2 + b^2 = c^2$

ディスプレイ数式: $$1 + 2 + \cdots + n = n(n+1)/2$$

`$...$` でインライン、`$$...$$` でディスプレイ数式を記述

---

## ダイアグラム — Mermaid

<Mermaid>
graph LR
  A[MDX] --> B[remark plugins]
  B --> C[rehype plugins]
  C --> D[React Components]
  D --> E[Presentation]
</Mermaid>

`<Mermaid>` コンポーネントでフローチャート・シーケンス図等を描画

---

---

## layout: section

# プレゼンテーション操作

キーボード・マウス・タッチ・ポインターで快適な発表体験

---

## 操作方法

| キー       | 機能                             |
| ---------- | -------------------------------- |
| **← →**    | スライド移動                     |
| **Space**  | 次のクリック / スライド          |
| **Escape** | 概要モード（サムネイルグリッド） |
| **f**      | フルスクリーン                   |
| **p**      | プレゼンターウィンドウ           |
| **d**      | 描画モード（フリーハンド注釈）   |
| **q**      | ポインターモード                 |
| **c**      | 描画クリア（描画モード中）       |

スワイプ操作・画面端クリックにも対応

---

## ツールと機能

::click

- **プレゼンターモード** — ノート + タイマー + 次スライドプレビュー、双方向操作対応（`p`）

::click

- **ポインターモード** — クリックでリップルエフェクト付きの視覚ポインター（`q`）

::click

- **描画モード** — フリーハンド注釈をスライドごとに保持（`d`）

::click

- **テーマ & PDF** — CSS 変数ベースのテーマ + `Ctrl+P` で PDF エクスポート

---

---

## layout: center

# reslide でプレゼンを始めよう

```
npm install @reslide-dev/core
```

Escape で概要モード / q でポインター / d で描画 / p でプレゼンター
